import { motion } from 'motion/react';

function MyComponent() {
  return (
    <motion.div animate={{ x: 100 }}>
      This is a component that is animated.
    </motion.div>
  );
}

export default MyComponent;
